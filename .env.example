# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_password
DB_NAME=chatopamobile_dev

# Application Configuration
APP_PORT=8080

# OPA.co.id Configuration (if needed for additional settings)
OPA_API_BASE_URL=https://api.opa.co.id